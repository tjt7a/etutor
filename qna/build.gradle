apply plugin: 'application'

mainClassName = 'ai.lucida.qa.QADaemon'

dependencies {
	compile project(':qna:OpenEphyra')
}

test {
	// OpenEphyra opens files relative to its project directory
	workingDir 'OpenEphyra'
}
